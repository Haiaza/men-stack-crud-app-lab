<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1> <% movie.name %> </h1>
    <% if (movie.snooze) { %>
        <p>Critics say <%= movie.name %> is awful. If you need to catch some Zzz's this is your movie!</p>
    <% } else {%>
        <p>Critics say <%= movie.name %> is amazing. Or atleast not going to put you to bed.</p>
    <% } %>
    <form action="/movies/<%= movie._id%>?_method=DELETE" method="POST">
        <button type="submit">
            Delete <%= movie.name %>
        </button>
    </form>
    <a href="/movies/<%= movie._id %>/edit">Edit <%= movie.name %></a>
    <a href="/movies">The Movie Catalog</a>
</body>
</html>